const products = ['Vaseline', 
                  'PlanB', 
                  'Condom', 
                  'Rose👀',
                  'Latex Suit'
                ];
const upperProducts = products.map(product => product.toUpperCase());
console.log(upperProducts);